import 'package:flutter/material.dart';

class ProdukDelete extends StatelessWidget {
  const ProdukDelete({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}